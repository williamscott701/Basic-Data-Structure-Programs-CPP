Create an abstract class Shape with one abstract method draw and one non-abstract method delete (details of methods mentioned below).
Create two classes for Circle and Elcric respectively which inherits the Shape class and implements its methods.
Create a separate class Lab1_<Roll_Number> which contains main method.
Create Objects for Circle and Elcric using reference of Shape class.
Take a string as input from console and call draw functions of both the Circle and Elcric objects. Call delete function of each of the objects.